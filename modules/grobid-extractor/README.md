# GROBID Extractor

Converts PDFs into TEI-like structured documents. In fixture/snapshot mode it is fully offline. In live/local mode, set `grobid_url` to a local GROBID service. The intended production path is a pinned Docker image recorded in `environment.lock`.

