# PDF Fetcher

Caches PDFs from open-access links. The module refuses non-OA and missing PDF links in `live_archived` mode. Fixture mode writes small deterministic placeholder PDFs for downstream parser tests.

