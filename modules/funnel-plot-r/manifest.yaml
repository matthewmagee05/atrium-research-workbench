id: funnel-plot-r
version: 0.1.0
name: Funnel Plot R
description: Computes a funnel plot spec and simple Egger-style asymmetry signal.
stage: analyze
runtime: r
entry: entry.R
runtime_version: ">=4.3"
dependencies:
  r: DESCRIPTION
inputs:
  - name: effects
    schema: schemas/effects.json
outputs:
  - name: funnel_plot
    schema: schemas/funnel_plot.json
    description: Funnel plot spec.
    output_kind: figure_spec
params_schema: schemas/params.json
llm:
  required: false
  budget: {max_tokens_per_run: 0, max_calls_per_run: 0, max_cost_usd_per_run: 0}
  prompts: []
determinism:
  level: deterministic
  guarantee: Fixture and snapshot modes are deterministic; live modes archive raw requests and responses when applicable.
  variance_sources: []
  reproduction_metrics: [schema_validity, row_count_agreement]
human_in_the_loop:
  enabled: false
  triggers: []
side_effects:
  network: false
  network_domains: []
  filesystem_write: true
  external_processes: false
author: Research Workbench
license: MIT
documentation_url: 

