# Bibliometrix R

Tier 1A R analysis module. This first implementation uses deterministic R aggregations and declares the `bibliometrix-r` module boundary; installing and invoking the CRAN `bibliometrix` package for richer analyses is deferred behind the same manifest contract.
